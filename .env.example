# Copy to .env and fill in. LIGHTNING loads these via python-dotenv at import time.

# Required: used by extractors (extraction/*.py) and decision rationale/counterfactual.
ANTHROPIC_API_KEY=

# Optional: override the default model (see src/lightning/const.py).
# LIGHTNING_MODEL=claude-sonnet-4-6
